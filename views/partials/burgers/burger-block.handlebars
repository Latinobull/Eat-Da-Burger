<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
        {{#if devoured}} Devour Me! {{else}} I've been Devoured!{{/if}}
    </button>

</li>